.venv/
__pycache__/
*.pyc
.DS_Store

# out/refs and out/cast are deliberately NOT ignored: cloned voices read them
# on every render, and regenerating them needs the 2.5 GB model. The rest of
# out/ is audition scratch.
out/samples/
out/ab/
out/edge_refs/
# serve.py copies templates/gallery.html here on every run.
out/index.html
